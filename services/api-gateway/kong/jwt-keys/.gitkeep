# JWT Keys Directory
# Place your RS256 public key here as public.pem
# NEVER commit private keys to version control!

# Generate keys with:
# openssl genrsa -out private.pem 2048
# openssl rsa -in private.pem -pubout -out public.pem
